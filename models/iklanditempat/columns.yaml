# ===================================
#  List Column Definitions
# ===================================

columns:
    penempatan:
        label: Tempat
        relation: tempat
        select: concat(nama, ' ', keterangan)